"use client";

import { useEffect, useRef } from 'react';
import { gsap } from '../utils/gsapPlugins';

const PageTransition = () => {
  // Component is now disabled - no transition animation will be shown
  return null;
};

export default PageTransition; 